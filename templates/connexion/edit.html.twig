{% extends 'base.html.twig' %}

{% block title %}Edit Connexion{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}" type="text/css"/>
    <link rel="stylesheet" href="{{ asset('css/style_inscription.css') }}" type="text/css"/>
{% endblock %}

{% block body %}
    <h1>Edit Connexion</h1>

    {{ include('connexion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('connexion_index') }}">back to list</a>

    {{ include('connexion/_delete_form.html.twig') }}
{% endblock %}
